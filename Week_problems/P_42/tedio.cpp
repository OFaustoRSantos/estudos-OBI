#include <bits/stdc++.h>
using namespace std;
using ll = long long;


int main(){
    ios::sync_with_stdio(0);
    cin.tie(NULL);
    
    // conjunto de n elementos, pensou em um inteiro c.
    // n° de pares (x,y) com 0<=x<=y<=c;
    // x+y está em s, mas y-x não está em s.
    
}
