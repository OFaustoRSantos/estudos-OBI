#include <iostream>
#include <vector>
using namespace std;


// unfinished
int damorida(){
    int N; // number of days 
    string S;
    // S have N uppercase characteres
    // character i-th = i-th dish of day
    // outputs:
    int min_X_days; // minimun days franscisco will visit
    int X_integers; // days Francisco chose to visit the restaurant

    cout << min_X_days;
    cout << X_integers;

    // P.S: In the case where there is more than one valid answer, the one with the smallest lexicographic order should be printed.
    return 0;
}